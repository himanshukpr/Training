# model
# single value
# multi value


# create statements
instagram_user_name = "Himanshu" #SNAKE CASE
# ldh : referenev varialbe (name of the storge container)
# rhs : data

followers = 300
# ldh : referenev varialbe (name of the storge container)
# rhs : data


# read statements

# it will store the name and the hashcode in the memory then it will refere to the HEAP where the actual data is stored
print(instagram_user_name, id (instagram_user_name), type(instagram_user_name))
print(followers, id(followers), type(followers))
